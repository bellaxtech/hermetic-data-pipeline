# =============================================================================
# Crawler module dependencies
# =============================================================================

# Core
scrapy>=2.11,<3.0

# HTTP / networking
httpx>=0.27,<1.0
aiohttp>=3.9,<4.0

# Data serialisation
pyarrow>=14.0,<16.0        # Parquet export (optional – pipeline degrades gracefully)

# HTML parsing
lxml>=5.1,<6.0
beautifulsoup4>=4.12,<5.0

# Utilities
python-dateutil>=2.8,<3.0
w3lib>=2.1,<3.0
protego>=0.3,<1.0          # robots.txt parser

# Testing / development (optional – for local runs)
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
